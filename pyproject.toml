[project]
name = "adventuregpt"
version = "0.1.0"
description = "A modern CLI text adventure game powered by GenAI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prompt-toolkit>=3.0.50",
    "textual>=3.0.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.1.0",
]
